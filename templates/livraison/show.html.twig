{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>EstLivree</th>
                <td>{{ livraison.estLivree ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ livraison.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ livraison.latitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livraison_index') }}">back to list</a>

    <a href="{{ path('livraison_edit', {'id': livraison.id}) }}">edit</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
